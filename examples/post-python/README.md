# Simple Echo function

```console
$ npm install
$ serverless deploy
$ serverless invoke -f echo --data '{"foo":"bar"}' -l
Serverless: Calling function: echo...
--------------------------------------------------------------------
{ foo: 'bar' }
$ servereless remove
```
